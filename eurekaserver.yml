server:
 port: 8070

eureka:
 instance:
   hostname: localhost
 client: 
   fetchRegistry: false
   registerWithEuerka: false
   serviceUrl:
     defaultZone: http://${euerka.instance.hostname}:${server.port}/eureka/
   
